apiVersion: v1
kind: Service
metadata:
  name: airflow-db
  namespace: book-project
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    app: airflow-db
  clusterIP: None
