apiVersion: v1
kind: Service
metadata:
  name: airflow-web
  namespace: book-project
spec:
  type: NodePort
  selector:
    app: airflow-web
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30008